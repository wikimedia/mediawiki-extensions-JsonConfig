{
  "name": "jsonconfig",
  "version": "0.0.0",
  "private": true,
  "description": "Build tools for the JsonConfig extension.",
  "scripts": {
    "test": "grunt test",
    "build-lib": ". ./bin/build.sh"
  },
  "devDependencies": {
    "grunt": "0.4.5",
    "grunt-banana-checker": "0.4.0",
    "grunt-cli": "0.1.13",
    "grunt-jsonlint": "1.0.7",
    "wmui-base": "git+https://phabricator.wikimedia.org/diffusion/WMUI/wikimedia-ui-base.git#v0.7.1"
  }
}
